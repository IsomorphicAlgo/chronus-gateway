# Documentation assets

Images for the repository README and docs. **Not** shipped inside crates.io packages (`docs/` lives at workspace root).

| File | Use |
|------|-----|
| [`vitro-trial-run.png`](vitro-trial-run.png) | README § Trial run (Vitro) — gateway + dashboard trial capture |

Source filename on disk may be `Vitro Trial Run.png` at repo root; the committed copy uses a URL-safe name.
